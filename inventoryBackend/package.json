{
  "name": "inventory-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -b tsconfig.node.json",
    "setup-db": "ts-node -r tsconfig-paths/register -P tsconfig.node.json src/scripts/setupDatabase.ts"
  },
  "dependencies": {
    "sqlite": "^5.1.1",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "@types/sqlite3": "^3.1.6",
    "ts-node": "^10.0.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.0.0"
  }
}
